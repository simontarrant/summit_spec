# PostgreSQL Docker Configuration
POSTGRES_USER=simontarrant
POSTGRES_PASSWORD=password
POSTGRES_DB=summit_spec
POSTGRES_PORT=5432

# Full connection string (used by migrations and apps)
# Note: Must match the values above - .env files don't support variable expansion
DATABASE_URL=postgresql://simontarrant:password@localhost:5432/summit_spec?sslmode=disable
